day47-ai-teacher /grading 페이지를 "자동 채점" → "과제 현황 대시보드"로 전환해줘.

## 배경
기존: 교사가 학생 답안을 수동 입력 → 채점 실행
변경: 학생이 AI Tutor에서 과제 제출 → 자동 채점됨 → 교사는 결과 확인만

## 변경 사항

### 1. 사이드바 메뉴 변경
- 변경 전: ✅ 자동 채점
- 변경 후: 📋 과제 현황

### 2. /grading 페이지 → 과제 현황 대시보드

#### 상단: 과제 목록
teacher_assignments 테이블에서 교사의 과제 목록 조회:
```typescript
const { data: assignments } = await supabase
  .from('teacher_assignments')
  .select('id, exam_content, student_ids, status, created_at')
  .eq('teacher_id', teacherId)
  .order('created_at', { ascending: false })
  .limit(10)
```

각 과제 카드:
- 생성일
- 문제 수 (exam_content에서 시험지 파트 파싱)
- 상태: active / closed
- 제출 현황: "2/4명 완료"

#### 과제 클릭 시: 학생별 제출 현황
student_submissions 테이블에서 해당 과제의 제출 현황:
```typescript
const { data: submissions } = await supabase
  .from('student_submissions')
  .select('id, student_id, status, answers, grading_result, score, submitted_at, graded_at')
  .eq('assignment_id', assignmentId)

// student_profiles와 조인하여 이름 표시
const { data: students } = await supabase
  .from('student_profiles')
  .select('user_id, display_name, level, avatar_emoji')
```

학생별 행:
| 학생 | 상태 | 제출일 | 채점 결과 |
|------|------|--------|-----------|
| 🏆 김상위 | ✅ 채점 완료 | 2/16 15:30 | [결과 보기] |
| 📚 이중위 | ⏳ 미제출 | - | - |
| ✏️ 박하위 | ✅ 채점 완료 | 2/16 16:00 | [결과 보기] |
| 🌱 최기초 | ⏳ 미제출 | - | - |

상태 표시:
- pending → ⏳ 미제출
- submitted → 📤 제출됨 (채점 대기)
- graded → ✅ 채점 완료

#### [결과 보기] 클릭 시: 채점 결과 모달
- grading_result 마크다운 렌더링 (KaTeX 포함)
- 학생 답안 (answers jsonb) 표시
- Dialog 모달로 표시

### 3. 기존 자동 채점 폼 제거
- GradingForm 컴포넌트 제거 (또는 미사용)
- n8n 채점 webhook 직접 호출 제거
- 교사가 수동으로 채점하는 UI 전부 제거

### 4. 테이블 정보
```sql
teacher_assignments (
  id serial PRIMARY KEY,
  teacher_id text NOT NULL,
  exam_id integer,
  exam_content text NOT NULL,
  student_ids text[] NOT NULL,
  status text DEFAULT 'active',
  due_date timestamp,
  created_at timestamp DEFAULT now()
)

student_submissions (
  id serial PRIMARY KEY,
  assignment_id integer REFERENCES teacher_assignments(id),
  student_id text NOT NULL,
  answers jsonb NOT NULL DEFAULT '[]'::jsonb,
  score integer,
  grading_result text,
  status text DEFAULT 'pending',
  submitted_at timestamp,
  graded_at timestamp,
  created_at timestamp DEFAULT now()
)

student_profiles (
  user_id text PRIMARY KEY,
  display_name text NOT NULL,
  level text NOT NULL,
  score_range text,
  avatar_emoji text DEFAULT '👤',
  created_at timestamp DEFAULT now()
)
```

완료 후 npm run build 통과 확인.